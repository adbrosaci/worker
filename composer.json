{
  "name": "adbros/worker",
  "description": "Simple interactive worker for doing hard jobs.",
  "keywords": [
    "console",
    "generator",
    "nette",
    "orm",
    "presenter",
    "utility"
  ],
  "type": "library",
  "license": "MIT",
  "require": {
    "php": ">=7.1",
    "nette/php-generator": "^2.4 || ^3.0",
    "nette/utils": "^2.4 || ^3.0",
    "symfony/console": "^3.3 || ^4.0 || ^5.0"
  },
  "require-dev": {
    "nette/di": "^2.4 || ^3.0",
    "ninjify/coding-standard": "^0.10",
    "ninjify/nunjuck": "^0.3",
    "php-parallel-lint/php-console-highlighter": "^0.4",
    "php-parallel-lint/php-parallel-lint": "^1.2",
    "phpstan/phpstan": "^0.12",
    "phpstan/phpstan-deprecation-rules": "^0.12",
    "phpstan/phpstan-nette": "^0.12",
    "phpstan/phpstan-strict-rules": "^0.12"
  },
  "suggest": {
    "contributte/console": "Best minimal console (symfony/console) to Nette Framework"
  },
  "autoload": {
    "psr-4": {
      "Adbros\\Worker\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Adbros\\Worker\\Tests\\": "tests/cases/"
    }
  },
  "scripts": {
    "cs": [
      "parallel-lint -e php,phpt --blame src tests --exclude tests/tmp",
      "phpcs --standard=ruleset.xml --extensions=php,phpt --encoding=utf-8 --colors -nsp src tests --ignore=tests/tmp"
    ],
    "cs-fix": [
      "phpcbf --standard=ruleset.xml --extensions=php,phpt --encoding=utf-8 --colors -nsp src tests --ignore=tests/tmp"
    ],
    "phpstan": [
      "phpstan analyse -l max -c phpstan.neon src tests"
    ],
    "test": [
      "tester -C tests"
    ],
    "coverage": [
      "tester -p phpdbg -C --coverage coverage.xml --coverage-src src tests/cases"
    ]
  }
}
